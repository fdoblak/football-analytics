# Stage 11B pass / reception baseline.
# Project-generated Opta-style thresholds — NOT official Opta.
# Automatic ceiling is provisional; confirmed requires manual review.

schema_version: 1
config_version: 1
pipeline_id: pass_reception_baseline
pipeline_version: 1
stage: "11B"

metric_origin: project_generated
definition_style: opta_style_metric_definition

automatic_ceiling: provisional
no_automatic_confirmed: true
owner_change_alone_is_not_completed_pass: true
cut_replay_gap_no_pass: true
no_opta_claim: true
no_real_video: true

input_eligibility:
  require_possession_transitions: true
  require_contact_refs: false
  allow_synthetic: true

thresholds:
  long_pass_distance_m: 30.0
  min_pass_distance_m: 1.0
  reception_window_us: 2_000_000

target:
  track_id: 1
  relationship: confirmed_target

safety:
  runtime_root: /home/fdoblak/workspace/passing_reception_checks
  no_overwrite: true
