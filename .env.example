# Stage 1C — example environment variable names only.
# Leave values empty. Do not paste real tokens, passwords, or paths with secrets.
# Copy to a gitignored `.env` outside tracked content if needed.

SOCCERNET_PASSWORD=
SOCCERNET_EMAIL=
HUGGINGFACE_TOKEN=
GOOGLE_DRIVE_CREDENTIALS_PATH=
GITHUB_TOKEN=
API_KEY=
