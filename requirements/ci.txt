# Lightweight CI dependencies (Stage 2D).
# Intentionally omits heavy CV/ML stacks (kept in the local Conda constraint file).
-r base.txt
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.15.22
black==26.5.1
isort==8.0.1
mypy==2.3.0
build==1.5.0
wheel==0.47.0
types-PyYAML>=6.0
